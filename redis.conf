# Basic Redis configuration
bind 0.0.0.0
port 6379
protected-mode no

# Performance tuning
maxmemory 512mb
maxmemory-policy allkeys-lru

# Persistence
save 900 1
save 300 10
save 60 10000

# Load modules
loadmodule /usr/lib/redis/modules/redisearch.so 